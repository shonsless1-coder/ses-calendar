import{seal,cookie}from'../../_lib.js';export async function onRequestGet({request,env}){const state=crypto.randomUUID(),verifier=crypto.randomUUID()+crypto.randomUUID(),challenge=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(verifier)).then(b=>btoa(String.fromCharCode(...new Uint8Array(b))).replaceAll('+','-').replaceAll('/','_').replaceAll('=','')),packed=await seal({state,verifier},env),u=new URL('https://accounts.google.com/o/oauth2/v2/auth');for(const[k,v]of Object.entries({client_id:env.GOOGLE_CLIENT_ID,redirect_uri:`${new URL(request.url).origin}/api/oauth/callback`,response_type:'code',scope:'openid email https://www.googleapis.com/auth/calendar.events',access_type:'offline',prompt:'consent',state,code_challenge:challenge,code_challenge_method:'S256'}))u.searchParams.set(k,v);return new Response(null,{status:302,headers:{location:u,'set-cookie':cookie('ses_oauth',packed,600)}})}
